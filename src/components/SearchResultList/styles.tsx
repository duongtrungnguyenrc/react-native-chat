import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    display: "flex",
    padding: 5,
  },
  resultList: {
    marginTop: 10,
    height: "100%",
  },
});

export default styles;
