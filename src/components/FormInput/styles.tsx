import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {},
  inputLabel: { marginBottom: 5, fontWeight: "500" },
  input: {
    width: "100%",
    borderColor: "#ddd",
    borderWidth: 1,
    borderRadius: 10,
    padding: 17,
    marginBottom: 10,
  },
});
export default styles;
