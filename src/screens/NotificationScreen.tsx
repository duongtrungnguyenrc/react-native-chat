import { AddFriendNotificationList } from "@/components";
import { SafeAreaView } from "react-native";

const NotificationScreen = () => {
  return (
    <SafeAreaView>
      <AddFriendNotificationList />
    </SafeAreaView>
  );
};
export default NotificationScreen;
